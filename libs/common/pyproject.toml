[project]
name = "pitwall-common"
version = "0.1.0"
description = "PitWall AI - Shared Common Utilities"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pitwall"]
